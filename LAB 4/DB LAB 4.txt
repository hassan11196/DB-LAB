SELECT * FROM emp
	WHERE deptno in (SELECT deptno FROM dept WHERE loc = 'DALLAS');
    

--Q1
SELECT ENAME,JOB,SAL,DNAME,GRADE FROM EMP,DEPT,SALGRADE  ORDER BY DNAME;
--Q2
SELECT *  FROM DEPT,EMP ;

--Q3
SELECT SAL,ENAME FROM EMP WHERE SAL  > (SELECT MIN(SAL) FROM EMP) ORDER BY SAL;

--Q4
SELECT DEPTNO FROM DEPT WHERE (DEPTNO NOT IN (SELECT  DEPTNO FROM EMP));



SELECT COUNT(ENAME) FROM EMP;
SELECT * FROM DEPT D RIGHT OUTER JOIN EMP E ON (D.DEPTNO = E.DEPTNO);
SELECT * FROM DEPT D, EMP E WHERE (D.DEPTNO = E.DEPTNO);

SELECT COUNT(ENAME) FROM EMP GROUP BY DEPTNO;
SELECT MAX(SAL) FROM EMP WHERE SAL < (SELECT MAX(SAL) FROM EMP);


SELECT DEPTNO FROM DEPT WHERE DEPTNO NOT IN (SELECT DEPTNO FROM EMP);
SELECT MAX(SAL) FROM EMP ;
SELECT COUNT(*) FROM EMP GROUP BY DEPTNO;

--Q15


--Q16
SELECT JOB,SUM(SAL) FROM (SELECT EMP.JOB, EMP.SAL FROM EMP,DEPT WHERE DEPT.DNAME <> 'SALES' AND EMP.SAL < 5000 AND DEPT.DEPTNO = EMP.DEPTNO ORDER BY SAL) GROUP BY JOB ; 

SELECT JOB,SUM (SAL) FROM EMP GROUP BY JOB;

--Q17
SELECT E.ENAME,M.ENAME FROM EMP E LEFT JOIN EMP M ON E.MGR = M.EMPNO;

--Q18
SELECT UNIQUE EMP.JOB,DEPT.LOC FROM EMP,DEPT WHERE EMP.DEPTNO = 30 AND  (DEPT.DEPTNO = EMP.DEPTNO);

--Q19
SELECT E.ENAME AS EMPLOYEE,E.EMPNO AS EMP#,M.ENAME AS MANAGER,E.MGR AS MANAGER# FROM EMP E,EMP M WHERE E.MGR = M.EMPNO;

--Q20
SELECT SAL,ENAME FROM EMP WHERE SAL = (SELECT MAX(SAL) FROM EMP WHERE SAL < (SELECT MAX(SAL) FROM EMP));

SELECT DEPTNO FROM DEPT NOT IN (SELECT * FROM EMP);

SELECT E.ENAME,M.ENAME FROM EMP E,EMP M WHERE E.MGR = M.EMPNO;
SELECT E.ENAME,M.ENAME FROM EMP E,EMP M WHERE E.SAL = M.SAL AND E.ENAME <> M.ENAME;